query GetCharacterDetail($id: ID!) {
  character(id: $id) {
    id
    name
    status
    species
    gender
    image
    created
    type
    origin {
      name
    }
    location {
      name
    }
    episode {
      id
      name
      episode
      air_date
    }
  }
}
